<% if object.errors.any? %>
    <div style="color: red">
      <h2>The form contains <%= pluralize(object.errors.count, "error") %></h2>

      <ul>
        <% object.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
<%end%>